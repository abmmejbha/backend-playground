import express from 'express';
import app from './app.js';

const port = 3000;
app.listen(port, () =>  {
    console.log(`Server is runnign at port http://localhost:${post} পোর্টে সার্ভার চলছে।`);
})

